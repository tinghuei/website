# 樂聯工業官方網站 - 部署指南

## 快速開始

### 1. 解壓檔案
```bash
unzip le-lian-industrial-web-complete.zip
cd le-lian-industrial-web
```

### 2. 安裝依賴
```bash
pnpm install
```

### 3. 設置環境變數
在新帳號的 Manus 平台中，系統會自動注入以下環境變數：
- `DATABASE_URL` - 資料庫連接字符串
- `JWT_SECRET` - 會話簽名密鑰
- `VITE_APP_ID` - OAuth 應用 ID
- `OAUTH_SERVER_URL` - OAuth 伺服器 URL
- `VITE_OAUTH_PORTAL_URL` - OAuth 登入入口 URL
- `OWNER_OPEN_ID` - 所有者 ID
- `OWNER_NAME` - 所有者名稱
- `BUILT_IN_FORGE_API_URL` - Manus API 基礎 URL
- `BUILT_IN_FORGE_API_KEY` - Manus API 密鑰
- `VITE_FRONTEND_FORGE_API_KEY` - 前端 API 密鑰
- `VITE_FRONTEND_FORGE_API_URL` - 前端 API URL

### 4. 初始化資料庫
```bash
pnpm db:push
```

### 5. 啟動開發伺服器
```bash
pnpm dev
```

## 項目結構

```
le-lian-industrial-web/
├── client/                          # React 前端代碼
│   ├── src/
│   │   ├── pages/                  # 頁面元件
│   │   │   ├── Home.tsx            # 首頁
│   │   │   ├── About.tsx           # 公司簡介
│   │   │   ├── Services.tsx        # 業務範疇
│   │   │   ├── Quality.tsx         # 品質管理
│   │   │   ├── Training.tsx        # 人才培育
│   │   │   ├── Contact.tsx         # 聯絡我們
│   │   │   ├── DetailedCompetencyAssessment.tsx  # 職能評估
│   │   │   └── AdminDashboard.tsx  # 管理員儀表板
│   │   ├── components/             # 可重用元件
│   │   ├── contexts/               # React Context
│   │   ├── hooks/                  # 自定義 Hook
│   │   ├── lib/                    # 工具函數
│   │   ├── App.tsx                 # 主應用元件
│   │   ├── main.tsx                # 入口文件
│   │   └── index.css               # 全局樣式
│   └── public/                     # 靜態資源
├── server/                          # Node.js 後端代碼
│   ├── _core/                      # 核心框架代碼
│   ├── db.ts                       # 資料庫查詢函數
│   ├── routers.ts                  # tRPC 路由定義
│   ├── storage.ts                  # S3 存儲助手
│   ├── index.ts                    # 伺服器入口
│   └── admin-dashboard.test.ts     # 測試文件
├── drizzle/                         # 資料庫 Schema
│   ├── schema.ts                   # 表定義
│   ├── relations.ts                # 表關係
│   └── migrations/                 # 遷移文件
├── shared/                          # 共享代碼
│   ├── types.ts                    # 共享類型
│   └── const.ts                    # 共享常數
├── package.json                    # 依賴項配置
├── tsconfig.json                   # TypeScript 配置
├── vite.config.ts                  # Vite 配置
├── vitest.config.ts                # 測試配置
├── drizzle.config.ts               # Drizzle 配置
└── todo.md                         # 項目待辦事項

```

## 核心功能

### 1. 企業官網
- 6 個主要頁面（首頁、公司簡介、業務範疇、品質管理、人才培育、聯絡我們）
- 完整的組織架構展示
- 聯絡表單與提交記錄

### 2. 職能評估系統
- 19 個職位的深度職能細項拆解
- 基於 iCAP 金屬製造業職能基準
- 員工自主評估功能
- 自動職能落差計算
- 評估結果持久化

### 3. 管理員儀表板
- 公司統計卡片（總員工數、部門數、平均完成度、平均缺口）
- 篩選與搜尋功能（按部門篩選、按員工名稱搜尋）
- 列表檢視 - 詳細評估結果表格
- 圖表檢視 - 部門職能對比分析、職能完成度分佈
- Excel 報表導出功能

## 資料庫表

### 主要表
- `users` - 用戶帳號
- `contactSubmissions` - 聯絡表單提交
- `assessmentResults` - 職能評估結果
- `jobPositions` - 職位定義
- `competencies` - 職能定義
- `positionCompetencies` - 職位職能要求

## API 端點

### 聯絡表單
- `POST /api/trpc/contact.submit` - 提交聯絡表單
- `GET /api/trpc/contact.getSubmissions` - 獲取提交記錄

### 職能評估
- `GET /api/trpc/competency.getPositions` - 獲取職位列表
- `GET /api/trpc/competency.getAllCompetencies` - 獲取職能列表
- `GET /api/trpc/competency.getPositionCompetencies` - 獲取職位職能要求

### 管理員儀表板
- `POST /api/trpc/adminDashboard.saveAssessment` - 保存評估結果
- `GET /api/trpc/adminDashboard.getAllResults` - 獲取所有結果
- `GET /api/trpc/adminDashboard.getByDepartment` - 按部門查詢
- `GET /api/trpc/adminDashboard.getByPosition` - 按職位查詢
- `GET /api/trpc/adminDashboard.getByEmployee` - 按員工查詢
- `GET /api/trpc/adminDashboard.getDepartmentStats` - 獲取部門統計
- `GET /api/trpc/adminDashboard.getCompanyStats` - 獲取公司統計

## 常用命令

```bash
# 開發
pnpm dev              # 啟動開發伺服器

# 構建
pnpm build            # 構建生產版本

# 測試
pnpm test             # 運行所有測試

# 資料庫
pnpm db:push          # 推送資料庫遷移

# 代碼格式化
pnpm format           # 格式化代碼
```

## 技術棧

- **前端框架**：React 19 + TypeScript
- **樣式**：Tailwind CSS 4
- **UI 元件**：shadcn/ui
- **後端框架**：Node.js + Express
- **API**：tRPC 11
- **資料庫**：MySQL/TiDB + Drizzle ORM
- **圖表**：Recharts
- **Excel 導出**：XLSX
- **構建工具**：Vite 7
- **測試**：Vitest

## 重要提示

### 環境變數
- 新帳號中的環境變數會自動注入，無需手動配置
- 不要在代碼中硬編碼敏感信息

### 資料庫
- 首次運行時，使用 `pnpm db:push` 自動創建所有表
- 資料庫 schema 定義在 `drizzle/schema.ts`
- 遷移文件自動生成在 `drizzle/migrations/`

### 靜態資源
- 不要將圖片、視頻等大文件存儲在 `client/public/`
- 使用 S3 存儲並通過 CDN URL 引用
- 使用 `storagePut()` 函數上傳文件到 S3

### 認證
- 使用 Manus OAuth 進行身份驗證
- 登入 URL 通過 `getLoginUrl()` 函數生成
- 用戶信息通過 `useAuth()` Hook 獲取

## 故障排除

### 資料庫連接失敗
1. 確認 `DATABASE_URL` 環境變數已正確設置
2. 檢查資料庫連接字符串格式
3. 確保資料庫伺服器可訪問

### 構建失敗
1. 清除 `node_modules` 並重新安裝：`rm -rf node_modules && pnpm install`
2. 清除 Vite 緩存：`rm -rf .vite`
3. 檢查 TypeScript 錯誤：`pnpm tsc --noEmit`

### 測試失敗
1. 確保資料庫已初始化：`pnpm db:push`
2. 檢查測試文件中的資料庫連接
3. 運行 `pnpm test` 查看詳細錯誤信息

## 後續開發

### 建議的功能擴展
1. **員工個人評估歷史** - 追蹤員工的評估進度
2. **部門負責人角色** - 實現基於角色的訪問控制
3. **評估結果對比** - 支援員工進度對比分析
4. **自動培訓計畫** - 根據評估結果自動生成培訓建議
5. **PDF 報表導出** - 支援導出為 PDF 格式

## 支持與聯絡

如有任何問題，請聯絡樂聯工業技術支持團隊。

---

**最後更新**：2026-04-13  
**版本**：06e662c4  
**狀態**：✅ 生產就緒
